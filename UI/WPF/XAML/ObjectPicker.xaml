<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <TextBox Name="txtObjectPickerSearch" Grid.Row="0" Margin="0,0,0,5"
             VerticalContentAlignment="Center" Padding="3,2,3,2" />

    <DataGrid Name="dgObjects"
        AutoGenerateColumns="False"
        SelectionMode="Single"
        SelectionUnit="FullRow"
        CanUserAddRows="False"
        VirtualizingPanel.ScrollUnit="Pixel"
        Grid.Row="1" />

    <StackPanel Grid.Row="2" Name="spObjectPickerSubMenu" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0" >
        <Button Name="btnObjectPickerOK" Content="OK" MinWidth="100" Margin="0,0,5,0" />
        <Button Name="btnObjectPickerCancel" Content="Cancel" MinWidth="100" />
    </StackPanel>
</Grid>